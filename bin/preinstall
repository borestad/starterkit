#!/usr/bin/env node -r esm

const exec = require('execa')
const ROOT = require('path').join(__dirname, '..')
const { log } = console

const run = cmd => {
  log(exec.shellSync(cmd).stdout)
}

run(`node ${ROOT}/node_modules/husky/husky.js install`)
