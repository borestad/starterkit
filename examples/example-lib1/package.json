{
  "name": "@scope/example-lib1",
  "private": true,
  "version": "1.0.0",
  "license": "UNLICENSED",
  "author": {
    "name": "NetEnt AB (publ)",
    "email": "info@netent.com",
    "url": "https://www.netent.com/"
  },
  "main": "src/index.ts",
  "scripts": {
    "build": "del dist && microbundle build src/index.ts -o dist --target web -f umd",
    "clean": "git clean -dxf -e node_modules",
    "compile": "del dist && tsc -b tsconfig.json --listEmittedFiles",
    "lint": "tslint -c ../../tslint.json --project tsconfig.json -t codeFrame --fix",
    "test": "jest --notify"
  },
  "dependencies": {
    "microbundle": "0.11.0",
    "typescript": "3.3.4000"
  }
}
