{
  "name": "@scope/example-cypress",
  "private": true,
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "clean": "ts-node -T ../../bin/clean.ts",
    "open": "cypress open",
    "build": "tsc -b tsconfig.json --pretty",
    "test": "run-p test:*",
    "test:e2e": "yarn clean && cypress run",
    "lint": "tslint -c ../../tslint.json --project tsconfig.json -t codeFrame"
  },
  "dependencies": {
    "cypress": "3.2.0",
    "typescript": "^3.4.5"
  }
}
