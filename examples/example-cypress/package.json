{
  "name": "@scope/example-cypress",
  "description": "Experimental e2e-testing with Cypress",
  "private": true,
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "clean": "sk-clean",
    "open": "cypress open",
    "build": "tsc -b tsconfig.json --pretty",
    "test": "yarn test:e2e",
    "test:e2e": "exit 0 && yarn clean && cypress run",
    "lint": "tslint -c ../../tslint.json --project tsconfig.json -t codeFrame"
  },
  "dependencies": {
    "cypress": "3.2.0",
    "typescript": "^3.4.5"
  }
}
